# Astra Mobile Worker Repo

Use this repository name: `astra-mobile-worker-src`.

Connect it to an existing Cloudflare Worker named `astra-mobile-worker` via:
`Settings -> Builds -> Connect`

Files:
- `src/index.js` — Worker code
- `wrangler.jsonc` — Worker configuration
